server:
  port: 8080

spring:
  application:
    name: campus-trade
  datasource:
    url: jdbc:mysql://rm-bp1gpy26lasmevofezo.mysql.rds.aliyuncs.com:3306/campus_trade?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: campus_user
    password: Ycc123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.entity
  configuration:
    map-underscore-to-camel-case: true

# 启动后自动打开主界面
app:
  browser:
    auto-open: true
  upload-dir: ${user.dir}/uploads

# JWT 配置
jwt:
  secret: campus-trade-jwt-secret-key-2024-demo
  expire-hours: 24
