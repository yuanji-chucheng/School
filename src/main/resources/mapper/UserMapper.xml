<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">
    <select id="findById" resultType="User">
        SELECT * FROM sys_user WHERE id = #{id}
    </select>
    <select id="findByStudentId" resultType="User">
        SELECT * FROM sys_user WHERE student_id = #{studentId}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user(student_id, password, nickname, avatar, role, status, credit_score)
        VALUES(#{studentId}, #{password}, #{nickname}, #{avatar}, #{role}, #{status}, #{creditScore})
    </insert>
    <update id="update">
        UPDATE sys_user
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="password != null">password = #{password},</if>
            <if test="status != null">status = #{status},</if>
            <if test="creditScore != null">credit_score = #{creditScore},</if>
        </set>
        WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM sys_user WHERE id = #{id} AND role = 0
    </delete>
    <select id="findPending" resultType="User">
        SELECT * FROM sys_user WHERE status = 0 ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>
    <select id="countPending" resultType="long">
        SELECT COUNT(*) FROM sys_user WHERE status = 0
    </select>
    <select id="findAll" resultType="User">
        SELECT * FROM sys_user ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>
    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM sys_user
    </select>
    <select id="findApprovedStudents" resultType="User">
        SELECT * FROM sys_user WHERE role = 0 AND status = 1 ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>
    <select id="countApprovedStudents" resultType="long">
        SELECT COUNT(*) FROM sys_user WHERE role = 0 AND status = 1
    </select>
</mapper>
